# Frontend environment variables
# Vite exposes only VITE_* prefixed variables to the browser.
# Set this to your backend URL in production. In dev, Vite proxies /api to localhost:5000.
VITE_API_URL=
